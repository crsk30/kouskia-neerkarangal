import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kouskia.neerkarangal',
  appName: 'kouskia-neerkarangal',
  webDir: 'www'
};

export default config;
